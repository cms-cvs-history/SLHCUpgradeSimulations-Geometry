process ProcessFastsimHitNtuplizer = {

  untracked PSet maxEvents = {untracked int32 input = -1}
  source = PoolSource { 
    //untracked vstring fileNames = {'file:/uscms_data/d1/cheung/slhc/test_muon_50GeV.root'}
    untracked vstring fileNames = {'file:/uscms_data/d1/cheung/slhc/timingtesta.root'}
  }

  # Initialize magnetic field
  include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
  es_module = TrackerDigiGeometryESModule { bool fromDDD = true }

  # Tracker SimGeometryXML
  include "Geometry/TrackerSimData/data/trackerSimGeometryXML.cfi"
  include "SLHCUpgradeSimulations/Geometry/data/strawmana/replace_cmsIdealGeometryXML.cfi"
  es_module = TrackerGeometricDetESModule { bool fromDDD = true }

  # Tracker Geometry Builder
  #include "Geometry/TrackerGeometryBuilder/data/trackerGeometry.cfi"

  # Tracker Numbering Builder
  #include "Geometry/TrackerNumberingBuilder/data/trackerNumberingGeometry.cfi"

        module ReadLocalMeasurement =  FastsimHitNtuplizer {
                string HitProducer = "siTrackerGaussianSmearingRecHits"
                untracked int32 VerbosityLevel = 1
	        //InputTag pixelsrc = siPixelRecHits
                //InputTag stripsrc1 = siStripMatchedRecHits:matchedRecHit
                //InputTag stripsrc2 = siStripMatchedRecHits:rphiRecHit
                //InputTag stripsrc3 = siStripMatchedRecHits:stereoRecHit
                string OutputFile = "fsgrechit_ntuple.root"
		# Following parameters for hit associator
  		//bool associatePixel = true
  		//bool associateStrip = true
                # need following line due to using updated file
                //bool associateRecoTracks = false
        }

	path p = {ReadLocalMeasurement}
}
